# 验证记录

> 本文档记录**实际执行过**的验证与结果，包括发现并修复的问题。
> 所有测试均可离线重跑：`npm test`（不需要模型额度）。

## 环境

| 项 | 值 |
|---|---|
| 主机 | Windows 11（MSYS/git-bash 终端） |
| Node.js | v22.23.2（`node:sqlite` 内置可用） |
| npm | 10.9.8（项目零依赖） |
| 模型接口 | `https://ollama.com/v1`（OpenAI 兼容），模型 `deepseek-v4.1-flash` |
| 密钥来源 | 本机已有 Hermes 配置（通过 `LLM_API_KEY_FILE` 引用，未复制明文进项目） |
| 数据库 | SQLite（`data/gengxia.db`，WAL 模式） |

## 一、自动化测试（共 170 项，全部通过）

| 套件 | 项数 | 结果 | 覆盖内容 |
|---|---|---|---|
| `node tests/run.js` | 55 | **55/55 通过** | Schema 校验、JSON 提取、输入校验、编辑补丁、HTTP 契约、权限、限流、体积限制、路径穿越、产物无密钥、持久化、重启后可用、关闭生成后仍可玩 |
| `node tests/engine.js` | 82 | **82/82 通过** | 真实 `engine.js` 在受控时钟下驱动：碰撞、生命、分数、无敌、暂停、重开、单次结算、帧率无关、输入、引擎硬限制、配置与玩法解耦 |
| `node tests/llm-errors.js` | 33 | **33/33 通过** | 假模型服务返回非法 JSON / 未知素材 ID / 超长文案 / 截断 / 空内容 / 429 / 500 / 401 / 超时 / XSS / 隐私 / 侮辱内容时的处理 |

## 二、验收标准逐项验证

| # | 验收项 | 验证方式 | 结果 |
|---|---|---|---|
| 1 | 首次访问者能理解输入什么、如何开始 | 真实浏览器打开首页：产品名、一句话介绍、示例（「老板一天改五遍需求」→《逃离改稿地狱》）、输入框、3 个示例入口、生成按钮齐全 | ✅ |
| 2 | 三条示例都能正常游玩、结算和重新开始 | 三个预设逐一在浏览器加载验证配置；引擎测试覆盖结算与重开逻辑；实机跑完整一局 | ✅ |
| 3 | 至少 10 条差异明显的输入经过真实生成测试 | **三轮共 42 次真实生成**（14 条 × 3 轮），全部记录见下表 | ✅ 42/42 成功 |
| 4 | 模型输出错误 JSON / 未知素材 ID / 超长文案 / 超时 / 限流的处理有效 | `tests/llm-errors.js` 用假模型服务逐一模拟 15 种异常 | ✅ 33/33 |
| 5 | 375px 手机与 1440px 桌面无关键遮挡；拖动不误触滚动 | CDP 设置真实视口测量：手机 stage 尺寸 263×467、比例 0.563（= 9/16）、完整可见、页面不可滚动；桌面无横向溢出 | ✅ |
| 6 | 碰撞、生命、分数、倒计时、无敌、暂停、重开正确；一局只结算一次 | `tests/engine.js` 确定性验证（含"结算后再推进 3 秒不重复结算"） | ✅ |
| 7 | 分享链接在独立会话打开呈现同一配置；重启后仍可访问 | 清空 localStorage 后打开分享链接：看到修改后的标题、可玩、无编辑按钮；杀进程重启后数据仍在 | ✅ |
| 8 | 关闭模型接入后，已有游戏仍可游玩 | 独立进程 `GEN_ENABLED=false` + 无密钥：预设可读、生成返回 503 `disabled` | ✅ |
| 9 | 前端产物与公开接口不含密钥、编辑凭据、原始私人故事 | 逐个文件 grep：8 个前端文件全部干净；`/api/health` 与 `/api/games/:id` 响应无密钥；数据库无 `story` 列，整段原始故事 0 命中 | ✅ |
| 10 | 无有效编辑凭据不能改他人游戏；生成接口有大小与频率限制 | 无凭据 PATCH → 403；错误凭据 → 403；他人游戏 → 403；越权后标题未变；超大请求体 → 413；超频 → 429 | ✅ |
| 11 | 构建与关键测试通过，无阻断功能的浏览器错误 | 无构建步骤（原生 JS）；三套测试 170 项全通过；真实浏览器操作全程无控制台错误 | ✅ |
| 12 | 如进行了公网部署，验证真实域名 | **未进行公网部署**（见"未验证项"） | ⏸ 未做 |

## 三、真实生成测试明细（42 次）

三轮生成，每轮 14 条差异明显的输入，3 并发。**42/42 全部成功，0 失败。**

| 轮次 | 模型 | 结果 | 耗时范围 | 中位耗时 |
|---|---|---|---|---|
| R1 | deepseek-v4.1-flash | 14/14 | 17.1–105.9 s | ~42 s |
| R2（提示词优化后） | deepseek-v4.1-flash | 14/14 | 7.7–20.9 s | 13.8 s |
| R3（主题路由优化后） | deepseek-v4.1-flash | 14/14 | 8–50 s（并发波动） | ~15 s |

### R3 生成结果（最终提示词版本）

| # | 输入类型 | 生成标题 | 主题 |
|---|---|---|---|
| 1 | 职场吐槽（老板改需求） | 凌晨两点的第五版 | office-neon |
| 2 | 生日祝福（奶茶+迟到） | 全糖去冰加珍珠 | birthday-pop |
| 3 | 宿舍整活（早八起不来） | 第七个闹钟 | morning-escape |
| 4 | 带娃日常（猫踩脸） | 六点踩脸闹钟 | morning-escape |
| 5 | 装修踩坑（拖延） | 明天就去 | office-neon |
| 6 | 减肥失败（炸鸡） | 这真的是最后一顿 | office-neon |
| 7 | 考研冲刺（抢座） | 六点半的座位 | morning-escape |
| 8 | 旅游翻车（下雨） | 泳衣白带了 | morning-escape |
| 9 | 相亲经历（七场） | 第七场相亲 | birthday-pop |
| 10 | 地铁通勤（挤） | 下一站，挤出去 | office-neon |
| 11 | 超长输入（约 550 字） | 十一点的关东煮 | office-neon |
| 12 | 短输入（下界附近 32 字） | 二楼也堵车 | office-neon |
| 13 | 游戏化题材（连败） | 稳住别慌，队长先冲 | office-neon |
| 14 | 家庭琐事（老爸钓鱼） | 两条鲫鱼讲一下午 | morning-escape |

**主题分布**：office-neon 8 / birthday-pop 2 / morning-escape 4 —— 三套主题都出现，且分配符合故事语义。

**内容质量抽查**（生日输入 → 生成配置）：

- 标题《全糖去冰加珍珠》、说明「帮小王收集全糖去冰加珍珠，躲开迟到的闹钟，撑到许愿结束再开喝。」
- 台词：「全糖去冰加珍珠，谢谢 / 又迟到了？蛋糕都等凉了 / 先许愿，奶茶一会儿再喝 / 别找了，奶茶在我们这儿 / 生日快乐，快吹蜡烛」
- 高分结算：「全糖去冰加珍珠，一口不剩！小王说这是他过得最甜的一个生日。」
- 图案匹配：障碍 `alarm-clock`（迟到闹钟）、奖励 `milk-tea`（奶茶）——与故事语义一致。

## 四、发现并修复的问题

测试驱动修复，全部复测通过：

1. **低帧率下游戏速度只有 1/10（严重）**
   现象：真实浏览器（约 1 fps 的无头环境）中 10 秒真实时间只推进 1 秒游戏时间，直接违反"速度不随帧率变化"。
   根因：`_tick` 里 `dtMs` 被硬性截断到 100 ms，低帧率下每帧只前进 100 ms。
   修复：上限放宽到 1000 ms（帧内分步已防止高速穿模），并把生成改为"补账"循环避免低帧率下实体变稀疏。
   复测：真实浏览器中 10 秒真实时间 → 推进 10000 ms 游戏时间（精确）。

2. **切到后台点击开始会进入"假运行"状态**
   现象：页面不可见时 rAF 不触发，但 `start()` 已把状态设为 `countdown`，用户看到"已开始但不动"。
   修复：`start()` 与 `resume()` 检查 `document.hidden` / `hasFocus()`，不可见时进入暂停态并给出对应文案。
   复测：真实浏览器中在隐藏标签页点击开始 → 正确显示"已暂停"，文案说明原因。

3. **游戏区域被压成近正方形（468×430），不是竖屏 9:16**
   现象：`aspect-ratio` 与 `max-height` 同时作用时比例被破坏。
   修复：宽度约束改为 `min(520px, 100%, calc(var(--stage-h) * 9/16))`，让比例始终成立；同时移除 `@media (min-width:1024px)` 里的宽度覆盖。
   复测：手机 375px 下 263×467（比例 0.563 = 9/16）；桌面下正确竖屏。

4. **畸形请求在服务未配置密钥时返回 503 而非 400**
   现象：`GEN_ENABLED`/密钥检查在请求体校验之前，导致过短故事、超大请求体等返回 503。
   修复：调整顺序——先做廉价的本地校验（400/413），再判断生成可用性（503）。
   复测：4 项相关测试全部通过。

5. **超大请求体导致连接异常（客户端 fetch failed 而非收到 413）**
   现象：`req.destroy()` 直接掐断连接，客户端拿到网络错误而不是 413。
   修复：改为 `req.pause()` + 响应头 `Connection: close`，并加 `clientError` 处理器兜底。
   复测：413 正确返回。

6. **结算页 HUD 穿透叠加层**
   现象：结算分数上方透出游戏中 HUD 的"分数"文字，视觉重叠。
   修复：叠加层出现时给 stage 加 `overlay-open` 类隐藏 HUD。
   复测：HUD 不透明度 0。

7. **叠加层内容未垂直居中（顶部密集、底部大片空白）**
   修复：用 `::before`/`::after` 弹性占位实现居中，同时保证内容过高时可滚动。
   复测：topGap 255 = bottomGap 255，完全居中。

8. **主角视觉尺寸明显小于收集物**
   修复：碰撞半径 0.042→0.048、绘制倍率 0.62→0.78。
   复测：截图中主角清晰可辨（眼镜、领带、工牌细节可见）。

9. **标题套路化（14 个里 6 个「大逃亡」）**
   修复：提示词加入"标题必须避免套路化"与五种命名方式示例。
   复测：修复后两轮共 28 个标题全部互不相同。

10. **主题选择偏斜（11/14 都用同一个绿色主题）**
    修复：主题说明从"场景分类"改为"视觉气质路由"并给出判断顺序。
    复测：三套主题分布 8/2/4，符合语义。

## 五、未验证项（明确说明）

| 项目 | 状态 | 原因 |
|---|---|---|
| 公网部署与真实域名验证（验收 #12） | **未做** | 任务书要求"有明确授权和可用部署环境时"才发布。当前未获得部署凭据与目标主机，**未用 localhost 链接冒充公网交付**。工程已包含 systemd / Docker / 反向代理部署说明与上线检查清单 |
| 真实手机设备上的触摸拖动 | **未做** | 无真机。已用 CDP 移动端视口（375×667）+ 触摸事件路径验证布局与 `touch-action: none`；引擎的指针拖动逻辑由 `tests/engine.js` 覆盖 |
| iOS Safari / 微信内置浏览器兼容性 | **未做** | 环境无这些浏览器。代码未使用实验性 API（`node:sqlite` 仅服务端；前端为标准 Canvas 2D + Pointer Events + WebAudio） |
| 多用户高并发压力测试 | **未做** | 仅验证了配置的并发闸门（3）与限流生效，未做真实负载压测 |
| 音效听感 | **部分** | 代码路径与静音开关已验证（`tests/engine.js` 记录音效调用序列），但无头浏览器无法实际出声，听感需人工试听 |

## 六、复现方式

```bash
cd gengxia
npm test                    # 三套测试串跑（55 + 82 + 33 = 170 项）

# 单独跑：
node tests/run.js           # Schema / API / 权限 / 限流
node tests/engine.js        # 引擎规则（无需浏览器）
node tests/llm-errors.js    # 模型输出异常与安全拦截

# 真实生成回归（消耗模型额度，谨慎重跑）：
PORT=8767 DB_PATH=./data/regression.db GEN_PER_IP_LIMIT=60 node server/index.js
```

引擎测试通过 `vm` 模块加载**真实的** `public/js/engine.js`，用假 canvas + 受控时钟驱动，
因此验证的是生产代码本身，而不是测试专用的替身实现。
