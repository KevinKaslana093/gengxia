# 第二轮开发说明（v1 → v2）

本轮在**现有工程上增量开发**，没有重建项目。所有 v1 数据与分享链接保持可用。

---

## 一、P0 问题修复（先修复，再扩展）

### P0-1 `/shared/schema.js` 返回 `text/html` —— 已修复

**根因**：`shared/` 位于项目根目录，而静态资源服务只映射 `public/`。请求 `/shared/schema.js` 找不到文件，
于是被 SPA 回退路由接管，返回了 `index.html`（881 字节）。浏览器把 HTML 当脚本加载，被 MIME 检查拒绝。

**修复**（`server/index.js` + `server/static.js`）：
1. 新增 `/shared/*` → 项目根 `shared/` 的显式映射（前后端仍共用同一份 Schema，不做文件复制）。
2. 新增 `looksLikeAsset()`：**带扩展名的资源请求找不到时返回真实 404**，不再被 SPA 回退伪装成 HTML。
3. 补全静态 MIME 表（`.js/.mjs/.css/.json/.svg/.map` 等）。

**验证**：
```
/shared/schema.js   → HTTP 200  text/javascript; charset=utf-8   (22,977 bytes)
/js/nope.js         → HTTP 404  text/plain
/shared/nope.js     → HTTP 404  text/plain
```
浏览器实测：10 个脚本全部正确加载，**控制台零错误**。

> 这个问题的教训已固化为测试：`tests/run.js` 的「静态资源可访问且类型正确」现在断言 **content-type**，
> 而不只是状态码（原先只断言 200，所以漏过了这个 bug）。

### P0-2 生成文案与游戏规则不一致 —— 已修复

**根因**：`intro` 由模型自由创作，可以虚构配置里不存在的机制对象。任务书举的猫的例子正是这样产生的
（简介说「追猫」，实际障碍却是「猫」）。

**修复（三重保障）**：

1. **规则句不再由模型生成**（`shared/schema.js` 的 `buildRuleText`）。
   `ruleText` 由服务端按玩法模板 + 配置里的真实对象名生成：
   - 躲避收集：`接住 {奖励} 得分，碰到 {障碍} 会扣血。`
   - 三路跑酷：`换道冲过 {奖励}，撞上 {障碍} 会掉血。`
   - 限时点击：`点掉 {障碍} 得分，点到 {奖励} 会扣血。`

   **模型输出里根本不存在 ruleText 字段**；即使模型硬塞一个也会被校验剥离（有测试覆盖）。
   这从结构上杜绝了「规则说明与引擎不符」。

2. **语义一致性检查**（`findSemanticConflict` / `collectSemanticConflicts`）。
   对 intro / title / lines / ending 逐个检查：
   动词是否绑定了它不该绑定的对象。检查用**最近绑定 + 动宾优先**判定：
   - 距离取动词最近的实体名（而非「附近有没有出现」）；
   - 距离相同时优先认为绑定动词**后面**的对象（中文动宾结构）。
   这两个规则缺一不可，否则「收集测试道具躲开测试障碍」会被误报成「收集了障碍」。

3. **场景素材白名单**：模型只能从所选场景列出的素材里挑，越界直接不合格并触发修复重试。

**修复后的猫示例**（重新生成，验收 #5）见 `eval/results-100.json` 中 `pet` 分类的记录。

### P0-3 玩法闭环复核 —— 已验证

开始 / 移动 / 计分 / 受伤 / 暂停 / 恢复 / 结束 / 重新开始全部通过（见第四节测试与浏览器记录）。
重点确认了：**一局只结算一次**、**暂停期间计时与实体冻结**、**重开彻底归零**（含 lane / rows / 输入 / 音频状态）。

### P0-4 分享页刷新与重启后读取 —— 已验证

分享页直接访问、浏览器刷新、**服务重启**三种情况都能读到同一配置。旧 v1 游戏（`/g/EFMeTMHL3wxe`）
在新版本下照常可玩（读取时自动迁移）。

---

## 二、三种玩法

| 玩法 | id | 时长 | 生命 | 操作 | 说明 |
|---|---|---|---|---|---|
| 躲避收集 | `dodge` | 45s | 3 | ←→ / A D / 拖动 | 接住落下的奖励，躲开障碍 |
| 三路跑酷 | `runner` | 45s | 3 | ←→ / A D / 左右滑动 | 三条跑道切换，冲向奖励、避开障碍 |
| 限时点击 | `click` | 35s | 3 | 鼠标点击 / 触摸 | 点掉目标得分，点到干扰项扣血 |

引擎硬约束（不随难度放宽）：
- 跑酷：**任何一排障碍都不能占满三条跑道**（保证可避开）；速度上限 520px/s；反应时间下限 700ms。
- 点击：目标半径 26–46px；目标必须完整在画布内且互不重叠（最小间距余量 ≥6px）；同屏目标 ≤14。
- 全部引擎使用时间增量更新，**不同帧率下速度一致**（实测 60fps vs 30fps 差异 <0.1%）。

生成页可选「自动匹配」或指定玩法；自动匹配只会选中实际支持的玩法。

---

## 三、12 套场景

每套场景包含：独立背景绘制（不是同一张网格换色）、专属配色、主角图案、≥2 奖励图案、≥2 障碍图案、
示例故事与文案，并声明支持的玩法。

| 场景 | id | 支持玩法 |
|---|---|---|
| 深夜卧室 | `bedroom-night` | dodge / runner / click |
| 办公室 | `office-night` | dodge / click / runner |
| 宿舍早八 | `dorm-morning` | dodge / runner / click |
| 生日派对 | `birthday-party` | dodge / click |
| 奶茶店 | `milk-tea-shop` | dodge / click |
| 通勤地铁 | `subway-commute` | dodge / runner / click |
| 健身房 | `gym` | dodge / runner / click |
| 考试教室 | `exam-classroom` | dodge / click |
| 厨房 | `kitchen` | dodge / click |
| 宠物客厅 | `pet-living-room` | dodge / click |
| 旅行机场 | `travel-airport` | dodge / runner |
| 购物快递 | `parcel-delivery` | dodge / click |

玩法覆盖：躲避收集 12 套场景 / 三路跑酷 6 套 / 限时点击 11 套。
每套场景均有 4 种奖励与 4 种障碍素材可选（`shared/scenes.js` 为单一事实来源）。

素材总计：12 个主角 + 27 个奖励图案 + 21 个障碍图案，全部为程序绘制的矢量图（Canvas 2D），
无图片文件、无外部字体、无付费素材。全部 12 套场景的配色通过 **WCAG ≥4.5:1** 对比度检查
（实测最低 7.71:1，出现在 `dorm-morning`）。

---

## 四、首页与手机体验

- 首屏顶部即为「一键试玩」卡片（直接进入预设游戏，不用等生成），玩法展示提前。
- 大标题缩短，示例入口区分「填入这个故事」与「直接玩预设」。
- 生成阶段文案改为用户语言：`正在读你的故事` / `正在设计你的游戏` / `正在准备关卡` / `正在保存`，
  不再出现「校验」「配置修复」等内部术语。
- 工程说明与服务状态移出主用户流程。
- **HUD 显示剩余秒数**（≤10 秒变红），底部进度条保留。
- 操作提示按设备能力区分：触屏只显示触摸说明，桌面显示键盘/鼠标说明。
- 布局在 375px / 390px 宽度下检查（标题、开始按钮、暂停、结算、分享卡）。
- 尊重 `prefers-reduced-motion`（关闭动画）。

---

## 五、分享与挑战

- 结算页「分享成绩卡」生成 **720×1040 PNG**：游戏名、分数、一句挑战语、结算评价、三个素材图标、二维码与链接。
- 二维码由项目内零依赖编码器 `public/js/qr.js` 生成（**不是模型绘制**），
  经真实解码器验证：9/9 个用例（含中文、长链接、L/M/Q/H 各纠错等级）解码结果与原文完全一致；
  并且**从浏览器渲染出的成绩卡图片里也能扫出链接**（cv2 + pyzbar 双解码器）。
  卡片二维码在本地链接与较长域名下均保持 ≥3 像素/模块，已固化为回归测试（4 种 URL 长度）。
- 底部提供「保存图片」「用系统分享」「复制链接」三种方式；系统分享不可用时自动降级为复制链接。
- 分享链接**不含**编辑凭据、不含原始故事；朋友打开即可直接开始同一配置的游戏。
- 挑战语明确写为「你能超过 N 分吗？」，属于朋友间挑战，不是防作弊排名。

### 朋友挑战流程（任务书第七节第 4 条）

分享链接带上发起人的分数：`/g/<id>?c=<分数>`（分数只在查询串里）。

| 环节 | 表现 |
|---|---|
| 朋友打开挑战链接 | 开始页显示「朋友挑战 · 发起人打了 N 分，你来试试能不能超过。」 |
| 朋友打完后 | 结算页显示对比：超过 →「超过了发起人的 N 分 🎉」；持平 →「打成平手」；未超过 →「还差 N 分」 |
| 朋友再分享 | 复制的链接与卡片二维码自动换成**朋友自己的分数**，形成接力 |
| 无挑战参数时 | 完全按普通分享链接工作（不显示任何挑战 UI） |

**明确不承诺的部分**：界面文案不声称「同样的关卡」——当前实现没有固定随机种子，
因此不保证相同种子对应相同障碍序列，也没有防作弊排名语义（卡上写的是朋友挑战数据）。
任务书里「如承诺公平比较才需固定种子」的条件未触发。

---

## 六、批量评测（100 条）

`scripts/eval-100.js` —— 覆盖短输入、长输入、多人物、宠物、工作、生日、隐晦表达、矛盾故事。

**首批（15 条，每种玩法 5 条并显式指定）**：15/15 结构合法，0 重试，标题 15/15 不重复，
玩法分布正好 5/5/5 —— 设计与接口确认无误后再跑正式批。

**正式批（100 条，自动匹配）**：`eval/results-100.json`

| 指标 | 结果 |
|---|---|
| 结构合法率 | **100% (100/100)**，0 失败 |
| 标题去重 | **100%** (100/100) |
| 玩法分布 | dodge 47 / click 40 / runner 13 |
| 场景覆盖 | **12/12 全部出现** |
| 类型覆盖 | 9 类（工作/宠物/宿舍/生日/家庭/旅行/健康/购物/隐晦）全部 100% 通过 |
| 耗时 | 平均 33.7 s，中位 29.7 s，p90 52.3 s，区间 7.3–115.8 s |
| 修复重试 | 3 条（校验/语义未过 → 反馈后重试成功） |

**语义一致性独立审核**（`scripts/audit-semantic.js`，6 个维度，与结构合法率**分开统计**）：
**100/100 通过**。审核器本身用 4 类注入的反例验证过（未知素材、跨场景素材、
ruleText 不同源、点击玩法语义反转），确认不是空转。

**第一次跑**（保留为对照 `eval/results-100-run1.json`）：结构 94%（6 条 `bad_response`），
玩法偏斜 dodge 82/100。两个问题都已定位并修复（详见 `VERIFICATION-v2.md` 第三节）：
1. 该模型是**推理模型**，思考 token 计入输出预算，4000 上限下约 1/3 调用会 `finish_reason=length` 且内容为空
   → `LLM_MAX_TOKENS` 提到 8000，并把「空内容」纳入可重试错误；
2. 我自己的提示词写了「多数生活故事用 dodge 最稳」→ 改为按故事核心动作三分路由。

**说明**：结构合法率与语义贴合度分开统计；服务未返回 token 用量，故该项标注 `unknown`，未做估算。

---

## 七、测试

六套测试，全部离线可跑（`npm test` 或 `node tests/<名字>.js`）：

| 测试 | 覆盖 | 数量 |
|---|---|---|
| `tests/run.js` | Schema / API / 限流 / 安全 / 静态资源类型 | 61 |
| `tests/engine.js` | 躲避收集引擎（时间增量、暂停、碰撞） | 82 |
| `tests/modes.js` | 跑酷可避开性 + 点击边界 + 帧率一致性 | 37 |
| `tests/llm-errors.js` | 模型异常路径（坏 JSON/超时/429/401/截断…） | 33 |
| `tests/semantic.js` | 文案与规则一致性（正例+反例+子串陷阱+核心名词） | 19 |
| `tests/sharecard.js` | 分享卡版式（互不重叠/不溢出）+ 二维码模块尺寸 | 16 |
| 合计 | | **248** |

辅助脚本（需要服务运行 / 消耗模型额度）：

| 脚本 | 用途 |
|---|---|
| `scripts/eval-100.js` | 100 条故事批量评测（`EVAL_PILOT=1` 为首批模式） |
| `scripts/audit-semantic.js` | 对评测结果做 6 维度语义审核（与结构合法率分开统计） |
| `scripts/check-cat-example.js` | 验收 #5：复检旧猫配置 + 重新生成并验证 |

---

## 八、未完成 / 已知限制

- **公网部署未做**：未获得部署凭据与目标主机。README 内含部署步骤与上线检查清单。
  当前交付为本地运行（`http://127.0.0.1:8765`），**不以本地链接冒充公网交付**。
- **真机触摸、iOS/微信兼容性未验证**：浏览器视口测试不能替代真实手机验证。
- 二维码纠错等级固定为 M；成绩卡尺寸固定 720×1040（未做多尺寸导出）。
- 挑战模式未做固定随机种子（当前不承诺「相同种子相同关卡序列」）。
