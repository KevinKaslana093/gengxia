# v2 验证记录

> 本文档记录**第二轮实际执行过**的验证与结果，包括发现并修复的问题。
> 第一轮的记录见 `VERIFICATION.md`。所有自动化测试均可离线重跑：`npm test`（不需要模型额度）。

## 环境

| 项 | 值 |
|---|---|
| 主机 | Windows 11（MSYS/git-bash 终端） |
| Node.js | v22.23.2（`node:sqlite` 内置可用） |
| npm | 10.9.8（项目零依赖） |
| 模型接口 | `https://ollama.com/v1`（OpenAI 兼容），模型 `deepseek-v4.1-flash` |
| 密钥来源 | 本机已有 Hermes 配置（通过 `LLM_API_KEY_FILE` 引用，未复制明文进项目） |
| 数据库 | SQLite（`data/gengxia.db`，WAL 模式） |

---

## 一、P0 修复证据

### P0-1 `/shared/schema.js` 的 MIME 错误 —— 修复前/后

修复前（复现任务书报告的现象）：
```
GET /shared/schema.js  → HTTP 200  text/html   (881 bytes，实际是 index.html)
浏览器控制台：Refused to execute script ... MIME type ('text/html') is not executable
```

修复后：
```
GET /shared/schema.js  → HTTP 200  text/javascript; charset=utf-8   (22,977 bytes)
GET /shared/scenes.js  → HTTP 200  text/javascript; charset=utf-8
GET /js/modes.js       → HTTP 200  text/javascript; charset=utf-8
GET /js/qr.js          → HTTP 200  text/javascript; charset=utf-8
GET /js/art-scenes.js  → HTTP 200  text/javascript; charset=utf-8
GET /js/nope.js        → HTTP 404  text/plain      （不再被回退路由伪装成 HTML）
GET /shared/nope.js    → HTTP 404  text/plain
```

真实浏览器加载首页，`performance.getEntriesByType('resource')` 显示 10 个脚本全部加载；
`window.onerror` / `unhandledrejection` 收集器**捕获到 0 条错误**（截图与记录见第六节）。

### P0-2 文案与规则一致性 —— 修复前/后

修复前（任务书给出的猫的例子 `/g/6gWcNHikgbCP`）：简介说「拿着猫条**追猫**」，实际障碍却是「三点开唱的**猫**」。
根因：`intro` 由模型自由创作，可虚构配置里不存在的机制。

修复后：
- 规则句改由**服务端模板生成**（模型输出里不存在 `ruleText` 字段），杜绝「模型另写一套规则」。
- 新增语义一致性检查，并在本轮**补上核心名词匹配**：
  实体名常写成「[修饰语]的[名词]」（如障碍「三点开唱的猫」），
  文案通常只写核心名词「猫」；只匹配完整名称会**漏判**这类真实矛盾。
  补上后检查器能正确报出旧配置的问题，且不误伤「猫条」里的「猫」这类子串。

**验证（`node scripts/check-cat-example.js`，需服务运行）：**

```
══ 1. 用 v2 检查器复检旧 v1 配置（应报出矛盾）══
    → 语义检查结果: ❌ 检出 1 处问题:
      - intro：文案把障碍「三点开唱的猫」写成要去「追」的对象，而它是要躲开的

══ 2. 重新生成猫的示例 ══
  新游戏地址: /g/0ewGR6t40KC2
    标题     : 凌晨三点的演唱会
    玩法/场景: click / pet-living-room
    简介     : 凌晨三点，猫准时开唱，睡意全无——点掉捣乱的那只，别碰猫条。
    规则句   : 点掉 三点开唱的猫 得分，点到 猫条 会扣血。
    玩家/障碍/收集: 铲屎官(cat-owner) / 三点开唱的猫(cat) / 猫条(cat-treat)

══ 3. 对新生成跑语义一致性检查 ══
  语义检查: ✅ 通过（无矛盾）
══ 4. 结构校验（v2 schema）══
  结构校验: ✅ 通过
```

新设计把「追猫」改成**点掉捣乱的猫、别碰猫条**（click 玩法），
简介、规则句、目标、结算四处完全自洽。

---

## 二、自动化测试（共 248 项，全部通过）

| 套件 | 项数 | 结果 | 覆盖内容 |
|---|---|---|---|
| `tests/run.js` | 61 | **61/61** | Schema 校验、JSON 提取、v1→v2 迁移、HTTP 契约、权限、限流、体积限制、路径穿越、静态资源 **content-type**、产物无密钥、持久化、重启后可用、关闭生成后仍可玩 |
| `tests/engine.js` | 82 | **82/82** | 躲避收集引擎：碰撞、生命、分数、无敌、暂停、重开、单次结算、帧率无关、输入、硬限制 |
| `tests/modes.js` | 37 | **37/37** | 跑酷**可避开性**（200 排生成 0 违规）、点击**边界与不重叠**（300 次生成 0 违规）、帧率一致性、暂停/重开 |
| `tests/llm-errors.js` | 33 | **33/33** | 假模型服务模拟 15 种异常：坏 JSON / 未知素材 / 超长 / 截断 / 空内容 / 429 / 500 / 401 / 超时 / XSS / 隐私 / 侮辱 |
| `tests/semantic.js` | 19 | **19/19** | 文案与规则一致性：12 个正例放行、7 个反例拦截、子串陷阱、**核心名词匹配**、多字段汇总 |
| `tests/sharecard.js` | 16 | **16/16** | 分享卡版式（五种组合下互不重叠、不溢出）+ **二维码模块尺寸**（4 种 URL 长度均 ≥3px/模块） |
| **合计** | **248** | **248/248** | |

`npm test` 会依次跑完全部六套并汇总（`tests/all.js`），任一失败则退出码非 0。

> `tests/modes.js` 曾有一处**测试自身的抖动**：断言「生成的排数 ≥ 10」，但该局玩家不操作、
> 会在随机时刻死亡（实际 7–9 排）。已改为断言不变量（记录有效、每排都可避开），
> 连续 8 次运行稳定 37/37。

---

## 三、真实生成评测

### 首批（15 条，每种玩法 5 条，显式指定玩法）

| 指标 | 结果 |
|---|---|
| 结构合法率 | **100%** (15/15) |
| 修复重试 | 0 条 |
| 标题去重 | **100%** (15/15) |
| 玩法分布 | dodge 5 / runner 5 / click 5（符合指定） |
| 耗时 | 平均 30.5 s，区间 8.8–42.7 s |

场景匹配抽查：《登机口还有二十分钟》→ travel-airport、runner；《年卡只去过两次》→ gym、runner；
《新粮才香》→ pet-living-room、click —— 均与故事语义一致。

### 正式批（100 条，自动匹配）

**第二次跑（修复后）：`eval/results-100.json`**

| 指标 | 结果 |
|---|---|
| 结构合法率 | **100% (100/100)**，0 失败 |
| 标题去重 | **100%** (100/100) |
| 玩法分布 | **dodge 47 / click 40 / runner 13** |
| 场景覆盖 | **12/12 全部出现**（office-night 20、pet-living-room 16、bedroom-night 14、birthday-party 10、dorm-morning 8、gym 7、parcel-delivery 7、travel-airport 5、kitchen 5、subway-commute 5、exam-classroom 2、milk-tea-shop 1） |
| 类型覆盖 | work 18 / pet 16 / dorm 14 / birthday 12 / home 12 / travel 10 / health 8 / shopping 6 / subtle 4，**全部 100% 通过** |
| 耗时 | 平均 33.7 s，p50 29.7 s，p90 52.3 s，区间 7.3–115.8 s |
| 修复重试 | 3 条（结构校验或语义检查未过 → 反馈后重试成功） |

**语义一致性独立审核**（`node scripts/audit-semantic.js eval/results-100.json`，
与结构合法率**分开统计**，共 6 个维度）：

| 维度 | 结果 |
|---|---|
| 文案一致性（简介/标题/台词/结算 vs 配置） | 100 条 0 冲突 |
| 场景支持该玩法 | 100/100 |
| 素材 ID 合法 | 100/100 |
| 素材属于该场景 | 100/100 |
| 图案有绘制实现 | 100/100 |
| `ruleText` 与配置同源（防模型另写规则） | 100/100 |
| **审核通过率** | **100% (100/100)** |

> **审核器本身经过反例验证**：注入「未知素材」「跨场景素材」「ruleText 不同源」
> 「点击玩法语义反转」四类坏样本后，审核器全部抓出（只放过 1 个我构造错误的伪反例，
> 经复核该样本在 click 语义下实际是正确的），证明检查不是空转。
>
> **统计口径说明**：结构合法率与语义贴合度**分开统计**；服务未返回 token 用量，
> 该项标注 `unknown`，**未做估算冒充实测**。

**第一次跑（`eval/results-100-run1.json`，保留作为对照）：**
结构合法率 **94%**（6 条失败全部是 `bad_response`），玩法分布 **dodge 82 / runner 7 / click 5**（严重偏斜）。

诊断与修复：
1. **失败原因**：`deepseek-v4.1-flash` 是推理模型，思考 token 计入 completion 预算。
   实测同一提示词连续三次调用：一次 `completion_tokens=4000`（用满上限、`finish_reason=length`、
   `content` 为空），另两次正常（568 / 565 字符）。
   → 修复：`LLM_MAX_TOKENS` 4000→8000（**上限不增加实际消耗**），并把「空内容」纳入可重试错误
   （它属于瞬时故障，不是配置错误）。
2. **玩法偏斜原因**：提示词里我写了「多数生活故事用 dodge 最稳」，模型照办了。
   → 修复：改为按「故事的核心动作」三分路由，并明确「三种玩法都同样常用，不要一律选 dodge」。

---

## 四、发现并修复的问题（本轮，测试驱动）

1. **`/shared/schema.js` 被回退路由伪装成 HTML（P0）** —— 见第一节。同时把「静态资源 content-type」写入测试，防止回归。
2. **`gym` 场景把奖励图案 `takeout-box` 当作障碍** —— 增加 `fried-chicken` 障碍图案并修正。
3. **`ruleText` 可被模型自由写入** —— 改为服务端模板生成，模型输出中该字段不存在；测试断言传入的 `ruleText` 会被剥离。
4. **语义检查器把「收集测试道具躲开测试障碍」误判为矛盾（误报）** —— 原实现按「动词附近有没有出现」判定；
   改为**最近绑定**（取距离最近的实体名）。
5. **语义检查器在距离相同时选错绑定对象（误报）** —— 增加**动宾优先**：距离相等时认为动词绑定它后面的对象。
   这两条缺一不可，否则中文正常表达会大量误报。
6. **`bad_response`（空内容）不重试** —— 推理模型偶尔把预算全花在思考上，属瞬时故障；
   实测约 1/3 的调用会这样。改为可重试（并保留上限）。
7. **提示词导致玩法偏斜 82%** —— 删除「多数故事用 dodge 最稳」，改为按核心动作三分路由。
8. **二维码 `toCanvas` 忽略 `x`/`y` 参数** —— 成绩卡上二维码画在了画布左上角而不是指定位置；
   修复为按 `translate(x,y)` 落位，并保证白底尺寸与二维码一致。
9. **分享卡文字压住二维码** —— 版式重排后仍与评价行重叠 7px；用 Node 假 canvas 精确复算各块包围盒后修正，
   并固化为 `tests/sharecard.js`（含 1/2/3 位数分数、超长标题、超长评语五种组合）。
10. **超长评语被缩到看不清** —— 改为超过一行时**折行**（最多两行），而不是一路缩小字号。
11. **`npm test` 只跑一套测试** —— 新增 `tests/all.js` 汇总六套，任一失败则退出码非 0。
12. **`toCanvas` 的纠错等级参数名** —— 调用方写成 `ecc`，实际接受 `ec`，导致纠错等级被静默忽略；
    已修正并由测试验证 L/M/Q/H 四个等级都生效。

---

## 五、交付物对应清单（对照任务书第九节）

| # | 要求 | 状态 | 位置 / 说明 |
|---|---|---|---|
| 1 | 修改后的完整工程及简明更新说明 | ✅ | 工程目录 + `UPDATE-v2.md` |
| 2 | P0 修复证据，控制台无资源加载错误 | ✅ | 本文第一节；浏览器 0 条错误 |
| 3 | 三种可完整游玩、结算、重开的玩法 + 功能表 | ✅ | `UPDATE-v2.md` 第二节；`tests/modes.js` |
| 4 | 十二套场景及其支持的玩法矩阵 | ✅ | `UPDATE-v2.md` 第三节 |
| 5 | 猫的示例重新生成并通过语义检查 | ✅ | `node scripts/check-cat-example.js`（复检旧配置检出矛盾 + 重新生成 `/g/0ewGR6t40KC2` 并通过） |
| 6 | 100 条生成评测的输入、脱敏输出、结果、失败分类 | ✅ | `eval/results-100.json` |
| 7 | 手机与桌面的首页/游戏/结算截图 + 浏览器验证记录 | ✅ | `screenshots/v2_*.png` + 本文第六节 |
| 8 | 旧链接回归、刷新、重启后读取、编辑权限、无模型凭据时可玩 | ✅ | 本节第七节 |
| 9 | 分享卡与链接验证；部署成功则提供真实地址 | ⚠️ 部分 | 分享卡与二维码已验证（含真实扫码）；**公网部署未做**，见第八节 |
| 10 | 剩余问题、已知限制、成本与下一步建议 | ✅ | 第八节 |

---

## 六、浏览器验证记录（真实 Chromium）

| 验证项 | 方式 | 结果 |
|---|---|---|
| 首页资源与控制台 | 加载后收集 `error` / `unhandledrejection` | 10 个脚本全部加载，**0 条错误** |
| 三种玩法可玩 | 逐个打开预设并真实操作（脚本注入真实指针/键盘事件） | 躲避收集：得分 10、收集 1、连击 1、坚持 11s、生命耗尽结算；三路跑酷：**得分 34、收集登机牌 3、连击 2、坚持 12s**、生命耗尽结算；限时点击：得分 120、连击 6、8 次真实点击 0 失误 |
| 结算与重开 | 走完一局后点「再玩一次」 | 重开后 `state=playing`、`score=0`、`lives=3`、`lane=1`、overlay 关闭（跑酷实测） |
| 分享卡二维码可扫 | 从浏览器渲染的卡片图片中用 **cv2 + pyzbar 双解码器**扫码 | **4/4 张卡片扫码成功**，解出内容与卡片链接完全一致 |
| 挑战链接（`?c=42`） | 真实浏览器打开 `...?c=42`，检查开始页 | 显示「朋友挑战 · 发起人打了 42 分，你来试试能不能超过。」；控制台 0 错误 |
| 挑战结算对比 | 打完后检查结算页 | 「发起人 42 分，还差 42 分」（实际得分 0，差值正确） |
| 挑战分享接力 | 结算页点「分享成绩卡」 | 卡片链接、按钮「复制链接」、卡片二维码**三处地址完全一致**（实测均为 `...?c=0`，即我自己的分数） |
| 挑战卡二维码 | 对新渲染的挑战卡扫码（zbar） | 解出 `http://127.0.0.1:8765/g/demo-naicha?c=0`，**与卡片链接一致** |
| 二维码编码正确性 | 9 个用例（含中文、长链接、L/M/Q/H 纠错） | **9/9 解码结果与原文字符串完全一致** |
| 二维码压力测试 | **29 个用例覆盖 QR 版本 1–10**（分享链接形态 × 4 种纠错级别 + 随机 ASCII 7 种长度 + 中文 UTF-8），渲染 PNG 后双解码器读 | **cv2 29/29、zbar 29/29，文本逐字节一致**；按级别 L 11/11、M 11/11、Q 4/4、H 3/3 |
| qr.js 代码规范 | 扫描箭头函数 / let·const / 模板字符串 / class | **违规 0 行**；UMD 包装与 `'use strict'` 均存在 |
| 二维码尺寸鲁棒性 | 独立生成 4 种长度 URL 的卡片二维码 PNG，双解码器扫 | 本地/短域名/长域名/超长（20 位 id + 挑战分）**全部解出且内容正确** |
| 手机 375px | CDP 视口 + 触摸媒体特性 | 首页无横向溢出；游戏页 stage 344×612（比例 0.563 = 9/16）；操作提示只显示触摸说明 |
| 手机 390px | 同上 | 无溢出；stage 362×644（0.563） |
| 桌面 1440×900 / 1366×768 | CDP 视口 | 无横向溢出；一键试玩卡片在首屏可见 |
| 设备自适应提示 | 模拟 `pointer:coarse+hover:none` 与 `pointer:fine+hover:hover` | 触摸设备只显示「手机：…」；桌面显示「电脑：… 手机：…」。**首页快捷键提示同样自适应**：手机只显示「10–1000 字」，桌面显示「10–1000 字 · Ctrl/⌘ + Enter 生成」 |
| 控制台错误（全流程） | 三个玩法 + 分享卡 + 手机/桌面切换 | 全程 0 条错误 |

---

## 七、分享链接 / 权限 / 重启（验收 #8）验证

本节每一项都是**实测**结果（命令与响应摘要）。

| 验证项 | 实测结果 |
|---|---|
| 旧 v1 链接回归 | `GET /g/EFMeTMHL3wxe` → **HTTP 200**；`GET /api/games/EFMeTMHL3wxe` → HTTP 200，标题《煎饼摊的加蛋奇迹》，读取时自动迁移为 `mode=dodge` / `sceneId=dorm-morning` / 服务端模板 `ruleText`「接住 阿姨的加蛋 得分，碰到 催命闹钟 会扣血。」 |
| 分享响应不含敏感数据 | 响应顶层键仅 `id / config / scene / mode / createdAt / source / editable`；`editToken`、`edit_token_hash`、`story`、`password`、`apiKey` **各出现 0 次** |
| 无凭据编辑 | `PATCH /api/games/EFMeTMHL3wxe` → **HTTP 403**，`{"code":"forbidden","message":"编辑凭据无效：只有创建者可以修改这款游戏"}` |
| 越权后标题未变 | 再次读取：标题仍是《煎饼摊的加蛋奇迹》 |
| 无模型凭据时已有游戏可玩 | 独立进程（`GEN_ENABLED=false` + `LLM_API_KEY_FILE` 指向不存在的文件）：health 报 `modelConfigured:false`、`generationEnabled:false`；旧 v1 游戏**照常读出且已迁移**；生成请求 → **HTTP 503 `disabled`**（明确报错，不伪装成功） |
| 启动日志不泄露密钥 | 未配置时打印「密钥：未配置 — 生成将返回错误，预设游戏仍可玩」；已配置时只打印来源形如 `file:.env:OLLAMA_API_KEY`，**从不打印密钥值** |
| 刷新与重启后读取 | 杀进程重启服务后，同一链接仍返回同一配置（多次重启验证） |

---

## 八、未完成项与已知限制

| 项目 | 状态 | 原因 |
|---|---|---|
| 公网部署与真实域名验证 | **未做** | 未获得部署凭据与目标主机。**未用 localhost 链接冒充公网交付**；README 含部署步骤与上线检查清单 |
| 真实手机设备触摸 / iOS / 微信兼容性 | **未做** | 无真机。已用 CDP 移动视口与触摸媒体特性验证布局与事件路径，但**浏览器视口测试不能冒充真机验证** |
| 语音/音效听感 | 部分 | 代码路径与静音开关由测试覆盖；无头环境无法出声，听感需人工试听 |
| 挑战模式固定随机种子 | **未做** | 当前「挑战我的分数」不承诺相同种子对应相同关卡序列，界面文案也未这样承诺 |
| 二维码纠错等级 | 固定 M | 未做按链接长度自动选择 |
| 评分标准 | 未做 | 「挑战我的分数」是朋友间挑战数据，非防作弊排名（界面已如此说明） |

### 运行成本（实测）
- 一次生成 ≈ 1 次模型调用；校验/语义未过时多 1 次修复调用；空内容时多 1 次重试。
- **本轮评测实际调用次数**（按评测记录里的 `attempts` 累加，不估算）：

| 批次 | 记录数 | 实际调用 |
|---|---|---|
| 首批（15 条） | 15 | 15 |
| 正式批（100 条） | 100 | 103 |
| 正式批第一轮（对照，保留） | 100 | 102 |
| **小计** | 215 | **220** |
| 另有调试/验收调用（诊断探针、猫示例重生成等） | — | 约 10 |
| **合计** | | **约 230 次** |

- 服务端保护（与 `.env` 实际值一致）：每日上限 **200**、单 IP **10 分钟 6 次**、
  全局并发 **3**、同故事 **2 分钟**去重、输出上限 **8000** token、重试上限 1 次（代码硬上限 2）。
- 服务未返回 token 用量，故**未统计 token 消耗**，也**未用估算值冒充实测**。

### 下一步建议
1. **公网部署**：需拿到部署凭据与目标主机；按 README 的 HTTPS 反向代理 + 持久磁盘方案，
   并把 `GEN_DAILY_LIMIT` 按预算调低。上线后须复验真实域名与 HTTPS（第一轮验收 #12）。
2. **真机验证**：真实手机触摸、iOS Safari、微信内置浏览器。当前只做了视口与事件路径验证。
3. **挑战模式加深**：若要承诺"公平比较"，需固定随机种子并让相同种子产出相同障碍序列；
   当前刻意不承诺，以免给出不成立的保证。
4. **成本观察**：`deepseek-v4.1-flash` 是推理模型，token 消耗以思考为主；
   若要压成本可考虑更小的非推理模型，或对短故事走更短的提示词。
5. **内容生产**：结构合法率与语义通过率均已达 100%（任务书目标 98% / 90%），
   再扩大内容量前建议先做多轮重复评测确认稳定性。
